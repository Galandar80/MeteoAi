const CACHE='meteo-ai-v47';
const CORE=['/','/index.html','/installa.html','/styles.css','/install.css','/pwa-install.js','/app-core.js','/app.js','/app-marine.js','/app-assistants.js','/app-features.js','/app-legal.js','/app-bootstrap.js','/world-live.html','/world-live.css','/world-live.js','/manifest.webmanifest','/icon.svg','/icon-192.png','/icon-512.png','/icon-maskable-512.png','/apple-touch-icon.png','/social-preview.jpg','/screenshots/app-desktop.png','/screenshots/app-mobile.png','/countries-110m.geojson'];
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE))));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k))))));
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;if(new URL(e.request.url).origin!==location.origin)return;e.respondWith(fetch(e.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy));return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match('/index.html'))))});
self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'/world-live.html',self.location.origin).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(windows=>{const existing=windows.find(client=>client.url.includes('world-live.html'));if(existing){if('navigate'in existing)existing.navigate(target);return existing.focus()}return clients.openWindow?clients.openWindow(target):undefined}))});
